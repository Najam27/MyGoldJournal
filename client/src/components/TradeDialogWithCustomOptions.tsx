import React, { useRef, useState } from "react";
import { ImagePlus, Plus } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { executionTypes, formatRr, levels, results, sessions } from "@/lib/gold";
import { trpc } from "@/lib/trpc";
import { toast } from "sonner";

function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="field"><span>{label}</span>{children}</label>; }
function Section({ title, children }: { title: string; children: React.ReactNode }) { return <section className="form-section modal-section"><span className="section-label">{title}</span><div className="field-grid">{children}</div></section>; }

function CustomSelect({ category, value, onChange, options, placeholder }: { category: string; value: string; onChange: (value: string) => void; options: string[]; placeholder?: string }) {
  const [draft, setDraft] = useState("");
  const optionQuery = trpc.optionLists.list.useQuery();
  const addOption = trpc.optionLists.add.useMutation();
  const utils = trpc.useUtils();
  const customs = (optionQuery.data ?? []).filter((item: any) => item.category === category && item.active).map((item: any) => item.value);
  const choices = Array.from(new Set([...options, ...customs]));
  const add = async () => { const next = draft.trim(); if (!next) return; try { await addOption.mutateAsync({ category, value: next }); await utils.optionLists.list.invalidate(); onChange(next); setDraft(""); toast.success(`${category} saved for future trades.`); } catch (error: any) { toast.error(error.message || `Could not save ${category}.`); } };
  return <div className="journal-value-control"><select value={value} onChange={event => onChange(event.target.value)}><option value="">{placeholder || `Select ${category.toLowerCase()}`}</option>{choices.map(item => <option key={item} value={item}>{item}</option>)}</select><div className="journal-value-add"><Input aria-label={`Add custom ${category}`} value={draft} onChange={event => setDraft(event.target.value)} placeholder={`+ Add ${category}`} /><button type="button" title={`Save custom ${category}`} aria-label={`Save custom ${category}`} disabled={addOption.isPending || !draft.trim()} onClick={() => void add()}><Plus size={14} /></button></div></div>;
}

function CustomText({ category, value, onChange, placeholder }: { category: string; value: string; onChange: (value: string) => void; placeholder: string }) {
  const [draft, setDraft] = useState("");
  const optionQuery = trpc.optionLists.list.useQuery();
  const addOption = trpc.optionLists.add.useMutation();
  const utils = trpc.useUtils();
  const customs = (optionQuery.data ?? []).filter((item: any) => item.category === category && item.active).map((item: any) => item.value);
  const add = async () => { const next = draft.trim(); if (!next) return; try { await addOption.mutateAsync({ category, value: next }); await utils.optionLists.list.invalidate(); onChange(next); setDraft(""); toast.success(`${category} saved for future trades.`); } catch (error: any) { toast.error(error.message || `Could not save ${category}.`); } };
  return <div className="journal-value-control"><Input value={value} placeholder={placeholder} onChange={event => onChange(event.target.value)} list={`journal-${category.replaceAll(" ", "-").toLowerCase()}`} /><datalist id={`journal-${category.replaceAll(" ", "-").toLowerCase()}`}>{customs.map(item => <option key={item} value={item} />)}</datalist><div className="journal-value-add"><Input aria-label={`Add custom ${category}`} value={draft} onChange={event => setDraft(event.target.value)} placeholder={`+ Add ${category}`} /><button type="button" title={`Save custom ${category}`} aria-label={`Save custom ${category}`} disabled={addOption.isPending || !draft.trim()} onClick={() => void add()}><Plus size={14} /></button></div></div>;
}

export function TradeDialogWithCustomOptions({ open, setOpen, form, setForm, editing, onSave, pending, screenshot, setScreenshot, progress }: any) {
  const fileRef = useRef<HTMLInputElement>(null);
  return <Dialog open={open} onOpenChange={setOpen}><DialogContent className="trade-dialog"><DialogHeader><DialogTitle>{editing ? "Edit trade" : "New trade"}</DialogTitle><DialogDescription>{editing ? "Update the journal detail and retain the original session." : "Session is detected from Pakistan Standard Time and can be overridden."}</DialogDescription></DialogHeader><p className="trade-custom-help">Use the <b>+ Add</b> control below any journal field to save a custom reusable option for future trades.</p><div className="trade-form"><Section title="Trade details"><Field label="Date"><Input type="date" value={form.tradeDate} onChange={event => setForm({ ...form, tradeDate: event.target.value })} /></Field><Field label="Session"><CustomSelect category="Session" value={form.session} onChange={session => setForm({ ...form, session })} options={sessions} /></Field><Field label="Direction"><select value={form.direction} onChange={event => setForm({ ...form, direction: event.target.value })}><option>BUY</option><option>SELL</option></select></Field><Field label="Result"><select value={form.result} onChange={event => setForm({ ...form, result: event.target.value })}>{results.map(item => <option key={item} value={item}>{item.replace("_", " ")}</option>)}</select></Field></Section><Section title="Strategy"><Field label="Level"><CustomSelect category="Level" value={form.level} onChange={level => setForm({ ...form, level })} options={levels} /></Field><Field label="Timeframe"><CustomSelect category="Timeframe" value={form.timeframe} onChange={timeframe => setForm({ ...form, timeframe })} options={["1m", "5m", "15m", "H1", "4H"]} /></Field><Field label="Setup quality"><CustomSelect category="Setup quality" value={form.setupQuality} onChange={setupQuality => setForm({ ...form, setupQuality })} options={["A+", "A", "B"]} /></Field><Field label="Confirmation"><CustomText category="Confirmation" value={form.confirmationType} onChange={confirmationType => setForm({ ...form, confirmationType })} placeholder="BOS, CHoCH…" /></Field></Section><Section title="Execution"><Field label="Execution type"><CustomSelect category="Execution type" value={form.executionType} onChange={executionType => setForm({ ...form, executionType })} options={executionTypes} /></Field><Field label="Market condition"><CustomText category="Market condition" value={form.marketCondition} onChange={marketCondition => setForm({ ...form, marketCondition })} placeholder="Bullish, ranging…" /></Field><Field label="Patience 1–5"><Input type="number" min="1" max="5" value={form.patienceScore} onChange={event => setForm({ ...form, patienceScore: event.target.value })} /></Field></Section><Section title="Risk"><Field label="Risk $"><Input type="number" min="0" step="0.01" value={form.risk} onChange={event => setForm({ ...form, risk: event.target.value })} /></Field><Field label="Reward $"><Input type="number" min="0" step="0.01" value={form.reward} onChange={event => setForm({ ...form, reward: event.target.value })} /></Field><Field label="P&L $"><Input type="number" step="0.01" value={form.pnl} onChange={event => setForm({ ...form, pnl: event.target.value })} /></Field><div className="rr-live"><span>LIVE R:R</span><strong className="data-text">{formatRr(form.risk, form.reward)}</strong></div></Section><Section title="Screenshot"><div className="upload-box" onClick={() => fileRef.current?.click()}><ImagePlus size={20} /><div><strong>{screenshot ? screenshot.name : "Click to upload a screenshot"}</strong><span>JPG, PNG or WEBP · 5MB maximum</span></div><input ref={fileRef} hidden type="file" accept="image/jpeg,image/png,image/webp" onChange={event => { const file = event.target.files?.[0]; if (!file) return; if (!["image/jpeg", "image/png", "image/webp"].includes(file.type)) { toast.error("Use a JPG, PNG, or WEBP screenshot."); return; } if (file.size > 5 * 1024 * 1024) { toast.error("Screenshot must be 5MB or smaller."); return; } setScreenshot(file); }} /></div>{progress > 0 && <div className="upload-progress"><i style={{ width: `${progress}%` }} /></div>}</Section><Section title="Emotions"><Field label="Before trade"><Textarea value={form.emotionBefore} placeholder="Calm, focused, dar raha tha…" onChange={event => setForm({ ...form, emotionBefore: event.target.value })} /></Field><Field label="During trade"><Textarea value={form.emotionDuring} placeholder="What were you thinking?" onChange={event => setForm({ ...form, emotionDuring: event.target.value })} /></Field><Field label="After trade"><Textarea value={form.emotionAfter} placeholder="Satisfied, gussa aya, should have held…" onChange={event => setForm({ ...form, emotionAfter: event.target.value })} /></Field></Section><Section title="Notes"><Textarea value={form.notes} rows={4} placeholder="English + Roman Urdu supported…" onChange={event => setForm({ ...form, notes: event.target.value })} /></Section></div><div className="dialog-actions"><Button variant="outline" onClick={() => setOpen(false)}>Cancel</Button><Button disabled={pending} onClick={onSave}>{pending ? "Saving…" : editing ? "Save changes" : "Save trade"}</Button></div></DialogContent></Dialog>;
}
