import React, { useMemo } from "react";
import { CheckCircle2, Clipboard, Download, ExternalLink, MonitorCog, PlayCircle, ShieldAlert, Wifi } from "lucide-react";
import { Button } from "@/components/ui/button";
import { downloadMt5Bridge } from "@/lib/mt5BridgeInstaller";

type Props = { bridgeUrl: string; targetName: string; targetReady: boolean; status: string; onTest: () => void; onImport: () => void; importing: boolean };

function Step({ number, title, children, state = "pending" }: { number: number; title: string; children: React.ReactNode; state?: "pending" | "ready" }) { return <article className={`mt5-guide-step ${state}`}><span className="mt5-step-number">{state === "ready" ? <CheckCircle2 size={15} /> : number}</span><div><h3>{title}</h3><div>{children}</div></div></article>; }

export function Mt5SetupGuide({ bridgeUrl, targetName, targetReady, status, onTest, onImport, importing }: Props) {
  const cleanUrl = bridgeUrl.trim().replace(/\/$/, "") || "http://localhost:7842";
  const statusUrl = `${cleanUrl}/status`;
  const urlWarning = useMemo(() => { try { const parsed = new URL(cleanUrl); return parsed.hostname !== "localhost" && parsed.hostname !== "127.0.0.1" ? "This bridge is configured away from localhost. The browser must be able to reach that address and the bridge must allow this app’s origin with CORS." : ""; } catch { return "The bridge URL is invalid. Use a complete address such as http://127.0.0.1:7842."; } }, [cleanUrl]);
  const copy = async () => { try { await navigator.clipboard.writeText(statusUrl); } catch { /* Clipboard may be unavailable in an embedded browser. */ } };
  return <section className="panel mt5-setup-guide"><div className="panel-title"><div><span>MT5 CONNECTOR</span><h3>Connect your MT5 terminal in five steps</h3></div><MonitorCog size={18} /></div><div className="mt5-guide-intro"><ShieldAlert size={18} /><p><strong>Important:</strong> Gold Journal cannot open MetaTrader 5 directly. It imports only from a small bridge service running in the <strong>same browser computer</strong> as the logged-in MT5 terminal. Mobile devices and a hosted browser cannot reach a bridge on your desktop unless that browser is running on the desktop itself.</p></div>{urlWarning && <div className="mt5-guide-warning"><ShieldAlert size={16} /><span>{urlWarning}</span></div>}<div className="mt5-guide-steps"><Step number={1} title="Open MetaTrader 5 on this PC"><p>Launch the MT5 desktop terminal, sign in to the exact trading account you want to journal, and confirm the terminal has an active connection. The bridge reads terminal data locally; never enter your MT5 password into Gold Journal.</p></Step><Step number={2} title="Download and start Gold Journal MT5 Connector"><p>Download the ready-made local connector, install its one prerequisite, then run it while MT5 stays open. It exposes the required <code>/status</code> and <code>/sync</code> endpoints and returns closed deals to Gold Journal for cloud import.</p><div className="mt5-connector-actions"><Button onClick={downloadMt5Bridge}><Download size={15} /> Download MT5 connector</Button><code>pip install MetaTrader5</code><code>python gold_journal_mt5_bridge.py</code></div><div className="mt5-endpoint"><code>GET {statusUrl}</code><button type="button" onClick={() => void copy()} aria-label="Copy bridge status URL"><Clipboard size={14} /></button><a href={statusUrl} target="_blank" rel="noreferrer" aria-label="Open bridge status endpoint"><ExternalLink size={14} /></a></div><small>The connector must return HTTP 200 and JSON such as <code>{'{"ok":true}'}</code>.</small></Step><Step number={3} title="Test the connection before importing" state={status.toLowerCase().includes("connected") ? "ready" : "pending"}><p>Use the test below. If it fails, keep MT5 and the connector running, confirm the exact URL and port, then allow that local port through the firewall. A browser “Failed to fetch” message means the page cannot reach the local bridge.</p><Button variant="outline" onClick={onTest}><Wifi size={15} /> Test bridge connection</Button></Step><Step number={4} title="Map the import to a journal account" state={targetReady ? "ready" : "pending"}><p>{targetReady ? <>Trades will import into <strong>{targetName}</strong>. Use the account selector in the live controls below if this needs to be a different Gold Journal account.</> : "Choose an existing Gold Journal account or create a dedicated account in the live controls below. The real import button stays disabled until this mapping is set."}</p></Step><Step number={5} title="Import closed history into Gold Journal"><p>Set the number of days, then import. The connector returns each closed MT5 deal to this browser, which saves it to your selected cloud journal. Ticket markers prevent the same deal from being added twice. After success, review Trade Log and Analysis.</p><Button disabled={importing} onClick={onImport}><PlayCircle size={16} /> {importing ? "Importing…" : "Go to live import controls"}</Button></Step></div><section className="mt5-troubleshooting"><div><span className="section-label">IF THE CONNECTOR DOES NOT WORK</span><h3>Read the matching recovery path</h3></div><div className="mt5-trouble-grid"><article><strong>“Failed to fetch” or connection refused</strong><p>The local connector is not running at this URL. Start the downloaded script, keep MT5 open, check the port, and allow that local port through the firewall.</p></article><article><strong>MetaTrader5 package or terminal error</strong><p>Run <code>pip install MetaTrader5</code>, then open and sign in to the Windows MT5 terminal before starting the connector again.</p></article><article><strong>CORS or browser security error</strong><p>Open Gold Journal in a browser on the same PC as the connector. The supplied connector includes local browser access headers; do not expose it publicly.</p></article><article><strong>HTTP 404 / 500 or invalid JSON</strong><p>The URL or connector contract is wrong. Confirm <code>/status</code> returns HTTP 200 JSON and start the downloaded Gold Journal connector again.</p></article><article><strong>Import says 0 added</strong><p>Ensure the selected MT5 account has closed deals in the chosen day range. Increase the history window, then run the import again. Existing ticket markers are correctly skipped.</p></article><article><strong>Trades land in the wrong journal</strong><p>Stop before importing, choose the correct target account in the live controls below, then retry. Account mapping is intentionally explicit to keep journals isolated.</p></article></div></section><footer className="mt5-guide-footer"><strong>Current bridge response:</strong><span>{status}</span></footer></section>;
}
